import { User } from "lucide-react";
import { ComingSoon } from "@/components/coming-soon";

export default function PerfilPage() {
  return (
    <ComingSoon
      title="Perfil"
      icon={User}
      description="Suas conquistas, preferências e histórico — em construção."
    />
  );
}
